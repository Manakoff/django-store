# 🛒 Store Server на Django

Добро пожаловать в репозиторий проекта **Store**! Это мой второй крупный практический проект, нацеленный на глубокое и всестороннее изучение веб-фреймворка **Django**, построение надежной архитектуры веб-приложений и интеграцию современных backend-технологий. 

Проект разработан с прицелом на коммерческое использование и демонстрирует навыки чистой архитектуры, безопасной авторизации, оптимизации запросов и построения API.

---

## 🛠️ Технический стек проекта
* **Backend:** Python 3.x, Django Web Framework, Django REST Framework (DRF)
* **Базы данных:** PostgreSQL (Production), SQLite (Разработка)
* **Асинхронные задачи & Кэширование:** Celery, Redis
* **Интерфейс & Шаблоны:** Django Templates (HTML/CSS), Работа со статикой и медиафайлами
* **Контроль версий & Workflow:** Git, GitHub, GitFlow (Ветвление)

---

## ⚙️ Методология управления репозиторием (Git)
В проекте строго соблюдается профессиональный подход к ведению контроля версий:
* **`main`** — стабильная ветка. Содержит только проверенный, протестированный и готовый к релизу код.
* **`develop`** — основная ветка разработки. Вся ежедневная работа над задачами, фиксация изменений и промежуточные коммиты происходят именно здесь.
* **Релизный цикл:** Слияние (Merge) ветки `develop` в ветку `main` происходит системно **один раз в неделю** после полного тестирования функционала текущего спринта.

---

## 📚 План реализации проекта и чек-лист задач

### 🔹 Этап 1: Базовая настройка и архитектура данных
- [x] **1. Начало с проектом и базовая настройка:**
    - Подготовил проект к работе: создал корневую директорию `store-server`, развернул виртуальное окружение `venv`, установил LTS-версию Django и инициализировал проект `store`.
    - Произвел тестовый запуск локального веб-сервера для проверки корректности установки фреймворка.
    - Создал и зарегистрировал изолированное Django-приложение `products` для управления каталогом товаров.
    - Организовал структуру директорий для хранения статических файлов и шаблонов, внес базовые настройки в `settings.py` и сконфигурировал главный файл диспетчера URL-адресов `urls.py`.
    - Написал базовые функции-обработчики (Views) для главной страницы и страницы каталога товаров в черновом режиме (без стилей).
    - Откорректировал пути к статическим файлам и адреса гиперссылок в меню для правильного динамического отображения стилей и работы навигации.
- [x] **2. Работа с шаблонами и статикой:**
    - Организовал вывод динамического контента на страницах `index` и `products` (список товаров, заголовок страницы).
    - Поработал с шаблонными тегами Django и placeholder'ами для гибкой разметки.
    - Реализовал в проекте правильные механизмы работы с URL-адресами через динамические теги.
    - Настроил полноценное наследование шаблонов в проекте (DRY архитектура).
    - Оптимизировал работу со статическими файлами, убрав статическое объявление и переделав на динамическое отображение.
- [x] **3. Django ORM, модели и БД:**
    - Создал модели `Product` и `ProductCategory` с полями для изображений и успешно применил миграции.
    - Поработал с созданными моделями через интерактивную консоль Django shell.
    - Создал суперпользователя, полностью настроил и кастомизировал административную панель.
    - Организовал выборку и работу с моделями в контроллерах (Views) и передачу данных в HTML-шаблоны.
    - Настроил конфигурацию проекта для корректного сохранения и отображения медиафайлов.
    - Создал изолированный диспетчер URL-адресов внутри приложения и скорректировал динамические URL в шаблонах.
    - Организовал первоначальное наполнение базы данных товарами через механизм загрузки фикстур (Django fixtures).

### 🔹 Этап 2: Бизнес-логика и пользовательский опыт
- [x] **4. Авторизация и регистрация:**
    - Создал кастомную модель пользователя с полем для аватарки и внес необходимые настройки в конфигурацию проекта (`AUTH_USER_MODEL`).
    - Реализовал механизмы аутентификации и авторизации пользователей.
    - Разработал функционал регистрации новых аккаунтов, а также механизм безопасного выхода из системы (`logout`).
    - Реализовал личный кабинет с возможностью редактирования профиля и загрузки изображения пользователя.
    - Настроил обработку ошибок (валидацию форм) для страниц входа и регистрации, а также добавил всплывающие сообщения об успешном создании аккаунта.
- [x] **5. Корзина товаров (Basket):**
    - Создал новую модель данных `Basket` для хранения товаров в корзине.
    - Реализовал вывод товаров через включенный шаблон `basket.html` внутри `profile.html`.
    - Разработал полноценный механизм добавления и удаления товаров из корзины.
    - Добавил к модели корзины методы `sum()`, `total_sum()` и `total_quantity()`, выведя данные в HTML.
    - Защитил доступ к корзине и личному кабинету с помощью декоратора `@login_required`.
- [x] **6. Фильтрация, пагинация и Django Admin:**
    - Кастомизировал административную панель Django (настроил отображение полей, поиск, фильтры и сортировку).
    - Реализовал фильтрацию товаров по категориям в каталоге интернет-магазина.
    - Внедрил постраничный вывод (пагинацию) списков товаров с помощью встроенного класса `Paginator`.

### 🔹 Этап 3: Продвинутый Backend и оптимизация

- [x] **7. CBV, миксины и Email-уведомления:**
    - Выполнил рефакторинг кодовой базы и перевёл контроллеры с функций (FBV) на классы (Class-Based Views).
    - Реализовал миксины (Mixins) для разграничения прав доступа и переиспользования логики авторизации.
    - Настроил отправку транзакционных email-уведомлений пользователям (например, для подтверждения регистрации).
- [x] **8. СУБД PostgreSQL и OAuth-авторизация:**
    - Осуществил переход со SQLite на промышленную реляционную СУБД PostgreSQL.
    - Перенёс данные и применил миграции на новой базе данных.
    - Интегрировал систему авторизации через сторонние социальные сети (OAuth protocol).
- [ ] **9. Тестирование, кэширование и асинхронные задачи (Celery/Redis):**
    - Написал unit-тесты для проверки ключевой бизнес-логики, моделей и контроллеров приложения.
    - Настроил кэширование данных для снижения нагрузки на базу данных и оптимизации времени отклика страниц.
    - Интегрировал Celery и Redis для выполнения «тяжёлых» фоновых задач (асинхронная отправка писем, обработка данных).

### 🔹 Этап 4: Финализация и API

- [ ] **10. Оформление заказов и интеграция оплаты:**
    - Спроектировал архитектуру данных и модели для создания и хранения заказов покупателей.
    - Разработал пошаговый процесс (checkout) и страницу оформления заказа.
    - Подключил и настроил внешний платежный шлюз для обработки онлайн-оплаты на сайте.
- [ ] **11. Конфигурирование проекта и безопасность:**
    - Изолировал все чувствительные данные и ключи доступа, вынеся их в переменные окружения (`.env`).
    - Разделил конфигурационные файлы проекта на конфигурацию для разработки (`development`) и продакшена (`production`).
    - Провёл финальный рефакторинг кода и оптимизацию SQL-запросов перед релизом.
- [ ] **12. Дополнительный модуль: Django REST Framework (DRF):**
    - Построил RESTful API для интеграции веб-приложения с мобильными или сторонними фронтенд-сервисами.
    - Разработал сериализаторы (Serializers) для преобразования моделей Django в JSON-формат.
    - Настроил безопасную аутентификацию для API (например, с использованием JWT-токенов).

---

## 🚀 Как запустить проект локально

1. Склонируйте репозиторий и перейдите в папку проекта:
   ```bash
   git clone https://github.com
   cd store-server/store
   ```
2. Разверните и активируйте виртуальное окружение:
   ```bash
   python -m venv venv
   # Для Windows:
   venv\Scripts\activate
   # Для macOS/Linux:
   source venv/bin/activate
   ```
3. Установите зависимости и примените миграции:
   ```bash
   pip install -r requirements.txt
   python manage.py migrate
   ```
4. Запустите локальный сервер:
   ```bash
   python manage.py runserver
   ```

---
⭐ *Данный проект демонстрирует мою готовность к решению реальных коммерческих backend-задач уровня Junior+.*
